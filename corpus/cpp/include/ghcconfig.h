/*
 * A stand-in for GHC's ghcconfig.h, for the benchmark corpus only.
 *
 * The real header does nothing but include the two below; so does this one.
 */

#pragma once

#include "ghcautoconf.h"
#include "ghcplatform.h"
